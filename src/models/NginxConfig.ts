export default interface NginxConfig {
    baseConfig: {
        byDefault: string
        customValue: string
    }
    alacranConfig: {
        byDefault: string
        customValue: string
    }
}
